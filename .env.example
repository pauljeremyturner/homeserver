PUID=1000
PGID=1000
TZ=Etc/UTC
PIHOLE_PASSWORD=changeme

# nowplaying: currently pointed at the temporary Rygel dev renderer running on
# the Fedora dev box (see project memory). Swap these to the real renderer's
# values once that hardware exists — no code change needed.
RENDERER_CONTROL_URL=http://192.168.1.148:55900/Control/Playbin/RygelAVTransport
RENDERER_SERVICE_TYPE=urn:schemas-upnp-org:service:AVTransport:2
NOWPLAYING_POLL_INTERVAL_SECONDS=3
